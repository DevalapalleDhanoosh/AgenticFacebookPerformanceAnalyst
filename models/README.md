# Models Folder

This folder stores any model artifacts, embeddings, vector stores, or checkpoints required by the agentic pipeline.

- `checkpoints/` → Any saved model weights
- `vector_store/` → Embedding or similarity search index
